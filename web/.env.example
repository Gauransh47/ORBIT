# Optional external ORBIT dataset host (no trailing slash required).
# Leave empty to load JSON from this app's /data path (web/public/data).
# Example: https://your-bucket.example.com/orbit-data
VITE_ORBIT_DATA_URL=
